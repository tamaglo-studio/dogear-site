# dogear-site
Chrome の標準履歴はタイトルと URL しか保存しません。Dogear は、実際に読んだ段落だけを自動検出して記録し、あとから内容で全文検索できる拡張機能です。スクロールで通過した部分は除外され、滞在した箇所のみを保存します。検索は本文にも対応し、ジャンル分類や簡易分析も可能。データはすべてローカル保存され、外部送信は行いません。JSON/CSVエクスポートにも対応しています。
